<%= form_for(@hangman_state) do |f| %>
  <% if @hangman_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hangman_state.errors.count, "error") %> prohibited this hangman_state from being saved:</h2>

      <ul>
      <% @hangman_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
