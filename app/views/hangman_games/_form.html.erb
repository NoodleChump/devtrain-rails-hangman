<%= form_for(@hangman_game) do |f| %>
  <% if @hangman_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hangman_game.errors.count, "error") %> prohibited this hangman_game from being saved:</h2>

      <ul>
      <% @hangman_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
