<% provide(:title, "Home") %>

<h1>Rails Hangman</h1>
<h2>Welcome to Hangman!</h2>

<%= button_to 'Start New Game', new_game_path, {:class => "btn btn-primary", :method => :get} %>
<br />
<%= button_to 'Create Custom Game', games_new_custom_path, {:class => "btn btn-primary", :method => :get} %>
