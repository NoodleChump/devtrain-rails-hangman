default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5

development:
  <<: *default
  database: rails_hangman_development
  host: localhost
  port: 5432

test:
  <<: *default
  database: rails_hangman_test

production:
  <<: *default
  database: rails_hangman_production
  username:
  password:
